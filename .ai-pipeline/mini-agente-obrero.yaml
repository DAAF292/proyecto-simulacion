agent:
  system_template: |
    You are a helpful assistant that can interact with a computer.
  instance_template: |
    Please solve this issue: {{task}}

    You can execute bash commands and edit files to implement the necessary changes.

    ## Recommended Workflow

    This workflow should be done step-by-step so that you can iterate on your changes and any possible problems.

    0. ANTES de tocar cualquier fichero de código: escribe tu plan real de implementación (qué ficheros vas a tocar, qué cambia en cada uno, en qué orden) sobrescribiendo el contenido de `docs/plans/in_progress/<nombre-de-esta-tarea>.md` -- ese fichero ya existe con el encargo original (puedes confirmar su nombre exacto con `ls docs/plans/in_progress/`); tu plan lo sustituye por completo. Comitéalo con su propio mensaje (`plan: <resumen>`) ANTES de escribir código. Este paso es obligatorio incluso para tareas pequeñas.
    1. Analyze the codebase by finding and reading relevant files
    2. Edit the source code directly to resolve the issue -- this project has a real, passing test suite already, so there is no need for a throwaway reproduction script; the task gives you the exact pytest command that serves as your reproduction and verification.
    3. If the task asks for new or updated tests, write them now, in the style of the existing tests in the file you're editing.
    4. While developing, run only the specific test file(s) you're working on (e.g. `pytest tests/test_foo.py -v`) -- not the whole suite. Run the FULL suite exactly once, right before you're done, to confirm no regressions.
    5. Test edge cases to ensure your fix is robust.
    6. Submit your changes and finish your work by issuing the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
       Do not combine it with any other command. <important>After this command, you cannot continue working on this task.</important>

    <important>
    This codebase's Python files are full of multi-line triple-quoted docstrings. Don't write standalone .py scripts to inspect, patch, or reproduce behavior -- a throwaway script with its own triple-quoted strings is a common source of syntax errors here. Edit directly: `sed -i` for small changes, a `cat <<'EOF' > file` heredoc for a whole function or file.
    </important>

    <important>
    Every command's output stays in your context for the rest of the run. Avoid re-running the same command more than once for the same purpose (e.g. running the full test suite repeatedly as a sanity check, or re-reading a file you've already seen) -- each repeat costs real money and doesn't teach you anything new.
    </important>

    ## Command Execution Rules

    You are operating in an environment where

    1. You issue at least one command
    2. The system executes the command(s) in a subshell
    3. You see the result(s)
    4. You write your next command(s)

    Each response should include:

    1. **Reasoning text** where you explain your analysis and plan
    2. At least one tool call with your command

    **CRITICAL REQUIREMENTS:**

    - Your response SHOULD include reasoning text explaining what you're doing
    - Your response MUST include AT LEAST ONE bash tool call
    - Directory or environment variable changes are not persistent. Every action is executed in a new subshell.
    - However, you can prefix any action with `MY_ENV_VAR=MY_VALUE cd /path/to/working/dir && ...` or write/load environment variables from files
    - Submit your changes and finish your work by issuing the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`.
      Do not combine it with any other command. <important>After this command, you cannot continue working on this task.</important>

    Example of a CORRECT response:
    <example_response>
    I need to understand the structure of the repository first. Let me check what files are in the current directory to get a better understanding of the codebase.

    [Makes bash tool call with {"command": "ls -la"} as arguments]
    </example_response>

    <system_information>
    {{system}} {{release}} {{version}} {{machine}}
    </system_information>

    ## Useful command examples

    ### Create a new file:

    ```bash
    cat <<'EOF' > newfile.py
    import numpy as np
    hello = "world"
    print(hello)
    EOF
    ```

    ### Edit files with sed:

    {%- if system == "Darwin" -%}
    <important>
    You are on MacOS. For all the below examples, you need to use `sed -i ''` instead of `sed -i`.
    </important>
    {%- endif -%}

    ```bash
    # Replace all occurrences
    sed -i 's/old_string/new_string/g' filename.py

    # Replace only first occurrence
    sed -i 's/old_string/new_string/' filename.py

    # Replace first occurrence on line 1
    sed -i '1s/old_string/new_string/' filename.py

    # Replace all occurrences in lines 1-10
    sed -i '1,10s/old_string/new_string/g' filename.py
    ```

    ### View file content:

    ```bash
    # View specific lines with numbers
    nl -ba filename.py | sed -n '10,20p'
    ```

    ### Any other command you want to run

    ```bash
    anything
    ```
  step_limit: 0
  cost_limit: 3.
  mode: confirm
environment:
  env:
    PAGER: cat
    MANPAGER: cat
    LESS: -R
    PIP_PROGRESS_BAR: 'off'
    TQDM_DISABLE: '1'
model:
  # Umbral bajado de 10000/5000+5000 a 4000/1500+1500 (2026-09-02, ver
  # guia-tareas.md "Coste real"): en un bucle agéntico, TODO lo que
  # quede en una observación se resube (a precio de caché, no gratis)
  # en cada paso siguiente -- el 96.8% del coste real medido en la
  # pieza de zoocoria era contexto repetido. Una salida verbosa (pytest
  # -v completo, un `cat` largo) que antes cabía entera por debajo de
  # 10000 caracteres ahora se recorta, reduciendo directamente cuánto
  # se re-factura en el resto de la ejecución.
  observation_template: |
    {%- if output.output | length < 4000 -%}
    {
      "returncode": {{ output.returncode }},
      "output": {{ output.output | tojson }}
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- else -%}
    {
      "returncode": {{ output.returncode }},
      "output_head": {{ output.output[:1500] | tojson }},
      "output_tail": {{ output.output[-1500:] | tojson }},
      "elided_chars": {{ output.output | length - 3000 }},
      "warning": "Output too long."
      {%- if output.exception_info %}, "exception_info": {{ output.exception_info | tojson }}{% endif %}
    }
    {%- endif -%}
  format_error_template: |
    {% if finish_reason is defined and (finish_reason == "length" or (finish_reason == "tool_calls" and not has_tool_calls)) -%}
    Your previous response reached the output token limit (finish_reason={{ finish_reason }}) before you produced a tool call, so it was cut off. Respond more concisely and finish with exactly one bash tool call. If you need to think more, do so briefly.
    {%- else -%}
    Tool call error:

    <error>
    {{error}}
    </error>

    Here is general guidance on how to submit correct toolcalls:

    Every response needs to use the 'bash' tool at least once to execute commands.

    Call the bash tool with your command as the argument:
    - Tool: bash
    - Arguments: {"command": "your_command_here"}

    If you want to end the task, please issue the following command: `echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT`
    without any other command.
    {%- endif %}
  model_kwargs:
    drop_params: true
